# Repository publication

Public repository: [shriyarv260/pfas-interface-dft](https://github.com/shriyarv260/pfas-interface-dft).

The user explicitly authorized public visibility. The connected GitHub account `shriyarv260` was verified; `shriya_v260` remains the supplied display/maintainer label. The repository description and source contents are published with the scientific status `not_validated` intact.

The local initial workflow commit is on `codex/initial-research-workflow`. GitHub publication uses `main`. Original software is MIT; literature-derived records retain the separate terms in `THIRD_PARTY_NOTICES.md`. No private credentials, pseudopotentials, or production PFAS calculations are bundled.

The May–August 2026 period is user-supplied project context. Commit dates reflect actual work; historical execution results are not reconstructed. Only revise scientific validation after independently reviewed computations and benchmarks exist.

The downloadable ZIP is a source snapshot without `.git`; initialize Git after extraction if using that copy.
